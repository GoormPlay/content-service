base-template:
  nameOverride: content-service
  fullnameOverride: content-service

  image:
    repository: roin09/content-service
    tag: latest

  service:
    port: 80
    targetPort: 8083

  env:
    useConfigMap: true
    useSecret: true

  replicaCount: 1

  ingress:
    enabled: 
    host: 

  externalSecret:
    enabled: true
    refreshInterval: 1h
    secretStore:
      name: aws-secrets
      kind: ClusterSecretStore
    dataFromKey: content-service-secret

  affinity:
    nodeAffinity:
      requiredDuringSchedulingIgnoredDuringExecution:
        nodeSelectorTerms:
          - matchExpressions:
              - key: node-type
                operator: In
                values:
                  - medium

  resources:
    requests:
      cpu: 300m
      memory: 384Mi
    limits:
      cpu: 500m
      memory: 512Mi
